/**
 * 화면에 노출되는 전역 문구.
 *
 * 값은 프로토타입(`docs/prototype.html`) 그대로다.
 * 프로젝트명은 "링고" 지만 화면 표기는 프로토타입을 따른다 —
 * 브랜드명이 바뀌면 이 파일만 고치면 된다.
 */

/** 헤더에 노출되는 브랜드명 */
export const BRAND_NAME = "맥주 파인더";

/** 문서 타이틀 겸 랜딩 헤드라인 */
export const APP_TITLE = "나에게 맞는 맥주 찾기";

/** 모바일 셸 최대 너비(px). globals.css 의 --container-shell 과 같은 값 */
export const SHELL_MAX_WIDTH = 390;
